{
    "name": "impact",
    "version": "0.0.1",
    "description": "Impact",
    "author": "Impact",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build:api": "cd packages/api && bun run build",
        "build:client": "cd packages/client && bun run build",
        "build": "bun run build:api && bun run build:client",

        "dev:api": "cd packages/api && bun run start:dev",
        "dev:client": "cd packages/client && bun run dev"
    }
}